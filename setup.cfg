[aliases]
build_docs = build_sphinx

[bdist_wheel]
universal = 1

[egg_info]
egg-base = build

[build_sphinx]
source_dir = src/doc
build_dir = build/doc

[upload_docs]
upload_dir = build/doc/html

[pytest]
norecursedirs =
    .git
    .bzr
    .hg
    .tox
    .cache
    .settings
    dist
    build
